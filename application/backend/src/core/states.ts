import {CryptoFees, CryptoPrices, DebounceCacheRequests, WSConnection} from '@type';

export const cryptoFees = {} as CryptoFees;
export const cryptoPrices = {} as CryptoPrices;

export const wsConnections: WSConnection[] = [];
export const debounceCache: DebounceCacheRequests = {};
